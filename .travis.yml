language: node_js
node_js:
  - "6.9.1"
  - "4.6.2"

# Install gulp-cli globally before installing all dependencies, including gulp
before_install:
  - npm i -g gulp-cli

# Running tests
script:
  - npm run test
  - npm run build
